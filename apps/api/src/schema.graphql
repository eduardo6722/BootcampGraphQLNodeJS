type User {
  id: ID!
  name: String!
  email: String!
}

input UserInput {
  name: String!
  email: String!
}

type Query {
  users: [User!]!
  findUser(params: QueryUser): [User!]!
}

input QueryUser {
  name: String!
}

type Mutation {
  createUser(params: UserInput): User!
}
